{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "npx tsc && node dist/index.js",
    "dev": "tsc-watch --onSuccess \"npm run compile\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.9.0",
    "tsc-watch": "^6.2.0",
    "typescript": "^5.6.3"
  }
}
